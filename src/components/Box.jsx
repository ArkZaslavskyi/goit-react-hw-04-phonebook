import styled from "@emotion/styled";
import { color, space, layout, flexbox, border, shadow } from 'styled-system';

const Box = styled('div')(
    color, space, layout, flexbox, border, shadow
);

export default Box;